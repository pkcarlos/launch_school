def palindrome?(obj)
  return obj == obj.reverse
end

# convert string to array
# remove non-alphanumerics (how?)
# revert to string, then reverse
def real_palindrome(obj)
  
end


(/[^0-9a-z]/i, '')  # regex